version: "3"

services: 
    angular-ui:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "4200:4200"
        volumes:
            - .:/app
        command: ng serve
